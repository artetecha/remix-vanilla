{
  "private": true,
  "sideEffects": false,
  "scripts": {
    "build": "remix build && npm run build:css ",
    "dev": "remix dev",
    "start": "remix-serve build",
    "build:css": "sass --load-path=node_modules styles/:app/styles/ --style=compressed"
  },
  "dependencies": {
    "@remix-run/node": "^1.6.5",
    "@remix-run/react": "^1.6.5",
    "@remix-run/serve": "^1.6.5",
    "sass": "^1.59.3",
    "vanilla-framework": "^3.12.1"
  },
  "devDependencies": {
    "@remix-run/dev": "^1.6.5",
    "@remix-run/eslint-config": "^1.6.5",
    "eslint": "^8.20.0"
  },
  "engines": {
    "node": ">=14"
  }
}
